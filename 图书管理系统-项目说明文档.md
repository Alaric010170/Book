# 图书管理系统 — 项目说明文档

> 课程作业项目 · Library Management System

---

## 一、项目背景

### 1.1 行业背景

中小规模图书室（如社区图书室、小型机构资料室、个人整理室）普遍存在以下痛点：

| 现状 | 问题 |
|------|------|
| 纸质台账手工登记借还 | 容易出错、难统计、无法实时掌握馆藏状态 |
| 图书分散摆放 | 不清楚哪本书在库、哪本书被借出、读者是谁 |
| 读者信息零散 | 无法按等级管理借阅权限、难以追踪借阅历史 |
| 借阅规则靠人记忆 | 续期次数、逾期罚息、借书上限等规则执行不一致 |
| 无法量化借阅热度 | 不知道哪些书最受欢迎、哪些书长期无人问津 |

### 1.2 项目定位

本系统是一个**面向中小型机构的图书借阅数字化管理系统**，核心目标：

- 用在线化台账替代纸质登记，所有数据可查、可统计、可追溯
- 提供高效的借还书操作流程，降低管理员的日常工作量
- 通过仪表盘让管理决策有数据依据（馆藏热度、借阅压力、逾期风险等）
- 容器化部署，一份代码、一键启动、随时迁移

### 1.3 项目目标拆解

```
项目成功标准
├── 业务可用
│   ├── 管理员可以流畅地完成 借 / 还 / 续 三个核心操作
│   ├── 图书和读者档案可以增删改查
│   └── 所有列表和统计查询响应时间 < 1s（加缓存）
├── 技术达标
│   ├── 前后端分离，接口契约清晰
│   ├── JWT 安全认证覆盖所有业务接口
│   ├── 支持 Docker Compose 一键启动全部服务
│   └── Redis 不可用时自动降级，不阻塞主流程
└── 工程规范
    ├── 后端多阶段构建，最终镜像仅 300MB 级别
    ├── 前端构建产物纯静态，由 Nginx 托管
    └── Git 提交历史干净，关键配置 .gitignore 覆盖
```

---

## 二、需求拆解

### 2.1 角色与权限

系统仅有 **管理员** 一种角色（课程作业简化设计），通过 JWT 认证保护全部业务接口。

| 功能 | 游客 | 管理员 |
|------|------|--------|
| 登录 | ✅ | ✅ |
| 查看仪表盘 | ❌ | ✅ |
| 图书增删改查 | ❌ | ✅ |
| 读者增删改查 | ❌ | ✅ |
| 借阅登记 / 归还 / 续期 | ❌ | ✅ |
| 通知中心 | ❌ | ✅ |
| 系统设置 | ❌ | ✅ |

### 2.2 核心业务流程

#### 2.2.1 借阅登记

```
输入：读者 + 图书
  │
  ├─ 校验 ① 读者可借数未达上限（普通 3 本 / VIP 10 本）
  │        ② 图书状态为"在库"
  │
  ▼
生成借阅记录
  ├─ 自动计算 应还日期（借出日期 + 默认期限）
  ├─ 读者 borrows 计数 +1
  ├─ 图书 status → "借出"
  └─ 生成系统通知
```

#### 2.2.2 归还登记

```
输入：借阅记录
  │
  ├─ 计算 逾期天数 = 今天 − 应还日期（若为负则 0）
  │
  ▼
更新借阅记录 status → "已归还"
  ├─ 读者 borrows 计数 −1
  ├─ 图书 status → "在库"
  └─ 生成系统通知（如逾期提醒）
```

#### 2.2.3 续期

```
输入：借阅记录
  │
  ├─ 校验：续期次数未超限制
  │
  ▼
更新 应还日期 = 当前应还日期 + 续期时长
  └─ 生成系统通知
```

### 2.3 功能需求清单（按模块）

#### M1 鉴权模块

| 编号 | 需求 | 优先级 |
|------|------|--------|
| M1-01 | 管理员通过用户名 + 密码登录 | P0 |
| M1-02 | 登录成功返回 JWT，前端存入 localStorage | P0 |
| M1-03 | 后续请求自动携带 Authorization 头 | P0 |
| M1-04 | 忘记密码流程（演示版验证码重置） | P1 |
| M1-05 | 未登录访问业务接口返回 401 | P0 |

#### M2 图书管理模块

| 编号 | 需求 | 优先级 |
|------|------|--------|
| M2-01 | 图书列表：全量展示 + 关键词搜索 | P0 |
| M2-02 | 图书列表：按分类筛选 | P0 |
| M2-03 | 图书列表：按状态（在库 / 借出）筛选 | P0 |
| M2-04 | 新增图书：书名、作者、ISBN、分类、状态、封面 | P0 |
| M2-05 | 编辑图书 | P0 |
| M2-06 | 删除图书 | P0 |

#### M3 读者管理模块

| 编号 | 需求 | 优先级 |
|------|------|--------|
| M3-01 | 读者列表：全量展示 + 关键词搜索 | P0 |
| M3-02 | 读者列表：按会员等级筛选 | P1 |
| M3-03 | 新增读者：ID、姓名、邮箱、等级、头像 | P0 |
| M3-04 | 编辑读者 | P0 |
| M3-05 | 删除读者 | P0 |
| M3-06 | 读者头像自动生成（DiceBear SVG） | P1 |

#### M4 借阅管理模块

| 编号 | 需求 | 优先级 |
|------|------|--------|
| M4-01 | 借阅登记：从可用图书列表 + 读者列表联动选择 | P0 |
| M4-02 | 归还登记：更新状态 + 计算逾期天数 | P0 |
| M4-03 | 续期：应还日期自动后移 | P0 |
| M4-04 | 借阅列表：按状态筛选（在借 / 已归还 / 逾期） | P0 |
| M4-05 | 借阅列表：关键词搜索 | P1 |

#### M5 仪表盘模块

| 编号 | 需求 | 优先级 |
|------|------|--------|
| M5-01 | 馆藏总数 + 在库数 + 逾期数 | P0 |
| M5-02 | 读者总数 + 会员数 + 会员占比 | P1 |
| M5-03 | 在借中 + 逾期 + 今日到期 | P0 |
| M5-04 | 热点数据缓存（Redis）减少数据库压力 | P1 |

#### M6 通知中心模块

| 编号 | 需求 | 优先级 |
|------|------|--------|
| M6-01 | 借阅 / 归还 / 续期 操作自动生成通知 | P0 |
| M6-02 | 通知列表展示 | P0 |
| M6-03 | 标记已读 / 全部已读 | P1 |
| M6-04 | 删除单条 / 清空全部 | P1 |

### 2.4 非功能需求

| 类别 | 需求 | 指标 |
|------|------|------|
| 性能 | 列表接口响应时间 | P95 < 1s（命中缓存 < 100ms） |
| 性能 | 仪表盘统计查询 | 通过 Redis 缓存，30s 内刷新 |
| 可靠性 | Redis 故障降级 | 不影响核心业务，自动回退数据库 |
| 可部署性 | 一键启动 | `docker compose up -d --build` 全量拉起 |
| 可移植性 | 环境变量注入 | DB / Redis / JWT 配置全部走环境变量 |
| 安全性 | 密码存储 | BCrypt 哈希，无明文 |
| 安全性 | 接口保护 | 除登录接口外，其余均需有效 JWT |

---

## 三、功能分析

### 3.1 功能模块总览

```
图书管理系统
│
├── M1 鉴权（Auth）
│   ├── POST /api/auth/login          → 返回 JWT
│   └── JwtAuthFilter                 → 拦截所有 /api/* （除 login）
│
├── M2 图书（Book）
│   ├── GET    /api/books             → 列表 + 关键词 + 分类 + 状态
│   ├── POST   /api/books             → 新增
│   ├── PUT    /api/books/{id}        → 更新
│   └── DELETE /api/books/{id}        → 删除
│
├── M3 读者（Reader）
│   ├── GET    /api/readers           → 列表 + 关键词 + 等级
│   ├── POST   /api/readers           → 新增
│   ├── PUT    /api/readers/{id}      → 更新
│   └── DELETE /api/readers/{id}      → 删除
│
├── M4 借阅（Borrowing）
│   ├── GET    /api/borrowings        → 列表 + 关键词 + 状态
│   ├── POST   /api/borrowings        → 登记借书
│   ├── POST   /api/borrowings/{id}/return  → 归还
│   └── POST   /api/borrowings/{id}/renew   → 续期
│
├── M5 仪表盘（Dashboard）
│   └── GET    /api/dashboard/stats   → 聚合统计（走 Redis 缓存）
│
└── M6 通知（Notification）
    ├── GET    /api/notifications             → 列表
    ├── POST   /api/notifications             → 新增
    ├── PATCH  /api/notifications/{id}/read  → 标记已读
    ├── PATCH  /api/notifications/read-all    → 全部已读
    ├── DELETE /api/notifications/{id}        → 删除单条
    └── DELETE /api/notifications             → 清空全部
```

### 3.2 前端页面与组件对应

| 路由 / 页面 | 主组件 | 核心交互 |
|-------------|--------|----------|
| /login | `LoginView.vue` | 登录表单 / 忘记密码 / 内嵌 SVG 图书馆背景 |
| /dashboard | `DashboardView.vue` + `Topbar.vue` + `Sidebar.vue` | 统计卡片展示 |
| /books | `BookCatalog.vue` | 图书列表 / 筛选 / 增删改弹框 |
| /readers | `ReaderManagement.vue` | 读者列表 / 筛选 / 增删改弹框（DiceBear 头像） |
| /borrowings | `BorrowingManagement.vue` | 借还续登记 + 状态列表 |
| /settings | `SettingsView.vue` | 管理员信息 / 头像预设 / 系统参数 |
| /help | `HelpView.vue` | 使用说明 |

所有页面共享 `Topbar.vue`（顶栏 + 通知入口）和 `Sidebar.vue`（左侧导航）。

### 3.3 后端分层架构

```
Controller 层（6 个）
  └── 接收 HTTP 请求，参数校验，调用 Service，返回 ApiResponse<T>

      ▼

Service 层（5 个）
  ├── 核心业务逻辑（借阅校验、续期、逾期计算）
  ├── @Cacheable / @CacheEvict 注解式缓存
  └── 写操作后自动跨模块清除关联缓存

      ▼

Mapper 层（5 个接口 + 对应 XML）
  └── MyBatis SQL 映射，驼峰自动下划线映射已开启

      ▼

MySQL 8.0（5 张表：t_admin / t_book / t_reader / t_borrowing / t_notification）
```

### 3.4 缓存失效策略

借阅操作是写入高频点，每次操作后必须保证列表和统计数据不会脏读：

| 操作 | 清除的缓存 key |
|------|---------------|
| 借阅登记 | `books` + `readers` + `borrowings` + `dashboard` |
| 归还登记 | `books` + `readers` + `borrowings` + `dashboard` |
| 续期 | `books` + `readers` + `borrowings` + `dashboard` |
| 图书 CRUD | `books` + `book-detail` |
| 读者 CRUD | `readers` + `reader-detail` |
| 通知变更 | `notifications` |

---

> 文档生成时间：2026-09-05
> 项目地址：https://github.com/Alaric010170/HomeWork
