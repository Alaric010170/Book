<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.mapper.AdminMapper">

    <resultMap id="AdminResult" type="com.library.entity.Admin">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="role" column="role"/>
        <result property="avatar" column="avatar"/>
    </resultMap>

    <select id="findByUsername" resultMap="AdminResult">
        SELECT * FROM t_admin WHERE username = #{username}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_admin (username, password, name, role, avatar)
        VALUES (#{username}, #{password}, #{name}, #{role}, #{avatar})
    </insert>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM t_admin
    </select>

</mapper>
